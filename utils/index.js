// why are these files named weirdly?
// because I don't like importing, say, `math` when I also might be using `Math`
import * as arr from './arr.js';
import * as dots from './dots.js';
import * as grid from './grid.js';
import * as maths from './maths.js';
import * as obj from './obj.js';

export {
  arr,
  dots,
  grid,
  maths,
  obj,
};

export default {
  arr,
  dots,
  grid,
  maths,
  obj,
};
